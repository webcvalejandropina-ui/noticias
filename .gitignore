node_modules/
.env
.env.local
dist/
*.log

# Build SSR (se genera en Docker con pnpm build; no versionar)
frontend/dist/

# Locks de otros gestores: el repo usa pnpm-lock.yaml
package-lock.json
frontend/package-lock.json
bun.lock
frontend/bun.lock

# Astro — caché local del proyecto
frontend/.astro/

# SQLite y WAL/SHM (se generan en runtime / Docker)
data/*
!data/.gitkeep

.DS_Store

# Vista previa generada solo en local (no versionar)
preview-local.png

# Export Docker local (docker save)
noticias-docker-images.tar
noticias-api-image.tar
noticias-frontend-image.tar
noticias-deploy.tar.gz
dist-deploy/
