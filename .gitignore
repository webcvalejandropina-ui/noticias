node_modules/
.env
.env.local
dist/
*.log

# Build SSR (se genera en Docker con npm run build; no versionar)
frontend/dist/

# El frontend usa Bun en repo; npm crea este lock al instalar con npm
frontend/package-lock.json

# Astro — caché local del proyecto
frontend/.astro/

# SQLite y WAL/SHM (se generan en runtime / Docker)
data/*
!data/.gitkeep

.DS_Store

# Vista previa generada solo en local (no versionar)
preview-local.png

# Export Docker local (docker save)
noticias-docker-images.tar
